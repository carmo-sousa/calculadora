const buttons = document.querySelectorAll(".buttons");

const buttonsArray = ["1", "2", "3", "4", "5", "6", "7", "8", "9", "0"];

buttons.forEach((button) => {});

// console.log(buttons[1].innerHTML);
